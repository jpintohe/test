import { Fragment } from 'react';
import Navigation from './routes/Navigation';

export const App = () => {
  return (
    <Fragment>
      <Navigation />
    </Fragment>
  );
};

export default App;
